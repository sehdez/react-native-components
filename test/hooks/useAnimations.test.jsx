import React from 'react'
import render from '@testing-library/react'
const { useAnimations } = require("../../src/hooks/useAnimations")

describe('Test useAnimation', () => { 
    test('getAnimation debe de retornar fadeIn, fadeOut, opacity, top' , () => { 
        
        // const { fadeIn, fadeOut, opacity, top } = useAnimations();
        render()
        // console.log( fadeIn, fadeOut, opacity, top )


     })
 })